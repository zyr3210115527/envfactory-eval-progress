# RL v4：预算条件化训练

一句话：把「上下文预算」从配置文件里的常数，变成每条 episode 随机采、模型看得见、超了直接判零的环境变量。

**这一版换掉了三样东西**：`MAX_TOOL_OBS_TOKENS`（删掉，工具返回按原始大小进上下文）、`max_steps`（不单独设上限，由预算隐式约束）、`-0.1·P_length`（整项删掉）。奖励只剩 `0.5·R_traj + 0.5·R_state`，超预算就 episode 立即终止、reward = 0，不是软扣分。

关键的一条是**把剩余预算写进每一轮观测**：`remaining_context: 41,203 / 65,536`。要模型学会节制，却不给它「还剩多少」的观测，信息上就是不可能的，它只能靠猜。给了这个字段，节制才从惩罚项间接雕出来的副产品，变成可以直接条件化的策略。

预算 B 每条 episode 开局采一次，log 均匀覆盖 8k / 16k / 32k / 64k / 128k，同一个 GRPO 组的 8 条 rollout 共用同一个 B（不然组内没法比）。

---

## 08-09 深夜 — 649 / 699 补上了：总分六个点单调下滑；我对机制改口太快，这条说清楚

第二批又回来两个点（649 job 689864、699 job 689877），都是完整 1112 条 sim / 278 任务，自检通过。

### 总分：六个点没有一次回头

| | 总分 | airline | retail | telecom |
|---|---|---|---|---|
| RL 前起点 8800base | 77.5% | 63.1% | 65.1% | 96.6% |
| SFT s3000 | 78.0% | 66.5% | 66.0% | 95.0% |
| v4.1 @499 | **79.2%** | 71.2% | 64.7% | 97.4% |
| v4.1 @549 | 79.1% | 73.5% | 62.1% | 98.9% |
| v4.1 @599 | 78.0% | 70.5% | 62.9% | 96.5% |
| v4.1 @649 | 76.5% | 69.3% | 60.7% | 95.8% |
| v4.1 @699 | 76.0% | 71.6% | 59.2% | 95.1% |
| v4.1 @999 | **73.2%** | 62.9% | 60.2% | 90.7% |

**79.2 → 79.1 → 78.0 → 76.5 → 76.0 → 73.2，单调下滑，六个点一次没回头。** 这是目前最扎实的一条，不依赖任何单点。

相对 v4.1 自己的 @499（**同端点、同 run 内部，不夹网关差异**）：@649 −2.70 [−5.39, **−0.00**]、@699 −3.27 [−6.34, −0.35]、@999 −6.06 [−9.28, −3.03]。**649 那条上界压在 0.00 上，是擦边，不当独立证据用**；699 和 999 结实。

相对 SFT 起点：@649 −1.45、@699 −2.01 都还**分不开**，到 @999 才是 −4.80 [−7.89, −1.70] 显著变差。**不是某一步突然塌，是持续滑，滑到 999 才滑出统计显著。**

### 机制：我一天里改口两次，这次讲清楚为什么

| | 写调用/对话 | 读调用/对话 | retail DB |
|---|---|---|---|
| SFT s3000 | 1.439 | 5.820 | 70.0% |
| @499 | 1.336 | 6.425 | 67.8% |
| @549 | 1.239 | 6.673 | 63.8% |
| @599 | 1.289 | 6.728 | 64.5% |
| @649 | 1.248 | 7.090 | 63.4% |
| **@699** | **1.173（谷底）** | **7.357（峰值）** | **61.8%** |
| **@999** | **1.343（反向）** | **6.782（反向）** | 63.3% |

**从 499 到 699 这五个连续点，读调用单调涨、写调用总体降、DB 总体跌，是一条干净的趋势**（@699 相对起点：写 −0.265 [−0.397, −0.138] 显著、读 +1.537 [+1.254, +1.844] 显著、DB −8.11 [−15.79, −0.66] 显著变差）。

**@999 在这两个量上同时反向**，但它的分数是最差的。

我前面两条的毛病记在这里：**第一次**（只有 3 个点）我把「写调用下降」当硬结论；**第二次**（加了 999）我说写调用不是病因、改推「读调用一路涨」；**第三次**（加了 649）读调用也不单调了，我又说两个量都在震荡、不该用单变量解释。

现在 699 补上，才看出前两次翻车的真正原因：**我一直拿 999 当「下一个点」在读，可它离 699 有 300 步，中间 749 / 799 / 899 三个点还没测。** 用一个隔了 300 步的点去判断趋势有没有拐，本来就不成立——这跟我在 240 步判据那条犯的是同一个错，只是换了个尺度。

**所以这次不下机制结论。** 749 / 799 / 899（job 689880 / 689887 / 689888）落地之前，「999 的反向是真拐点还是离群点」没法回答。能确定的只有总分那条单调下滑，以及 retail DB 从 549 起就钉在 61~64% 再没回去过。

### 一个数据质量上的小信号

未判分条数：@499 是 5、@649 是 9、@699 是 14、@999 是 9（分母都是 1112，最高 1.26%）。有轻微上漂但不单调，量级也还小，**已按约定单列、没填 0、没丢弃**。先记着，等剩下三个点看是不是趋势。

---

## 08-09 21:30 — 999 回来了：不是掉分变慢，是掉穿了；上一条的机制读法我改口

> ⚠️ 这条里「读调用一路涨」是当时只有四个点时的读法，**649 / 699 补上后已不成立**，见上面那条。总分结论不受影响。

第二批第一个点（iter 999，job 689892）跑完，1112 条 sim、278 个任务，跟历史同批完全对齐。

**它回答的那个问题，答案跟我预设的两个选项都不一样。**

上一条我给的判据是：「写调用继续掉 = reward 结构性问题；触底回升 = 一次性代价，可以继续跑。」

| 模型 | 写调用/对话 | 读调用/对话 | retail DB |
|---|---|---|---|
| SFT s3000 | 1.439 | 5.820 | 70.0% |
| v4.1 @499 | 1.336 | 6.425 | 67.8% |
| v4.1 @549 | **1.239**（谷底） | 6.673 | 63.8% |
| v4.1 @599 | 1.289 | 6.728 | 64.5% |
| **v4.1 @999** | **1.343（回升了）** | **6.782** | **63.3%** |

**写调用确实触底回升了**——549 谷底 1.239，999 回到 1.343，跟 @499 一个水平，相对 SFT 起点已经**不显著**（−0.096 [−0.219, +0.020]）。按我上一条定的判据，这该读成「一次性代价，可以继续跑」。

**但分数同时掉穿了。** 所以那条判据本身就是错的，写调用不是该盯的那个变量：

| | 总分 | airline | retail | telecom |
|---|---|---|---|---|
| RL 前的起点 8800base | 77.5% | 63.1% | 65.1% | 96.6% |
| SFT 收官 s3000 | 78.0% | 66.5% | 66.0% | 95.0% |
| v4.1 @499 | 79.2% | 71.2% | 64.7% | 97.4% |
| **v4.1 @999** | **73.2%** | 62.9% | 60.2% | 90.7% |

- 相对 SFT 起点 **−4.80 [−7.89, −1.70]，显著变差**。
- 相对 v4.1 自己的 @499 **−6.06 [−9.28, −3.03]，显著变差**。这条是**同端点、同一个 run 内部的训练增量**，不夹网关差异，是这份数据里最干净的一条。
- 已经**低于 RL 前的起点 8800base**（77.5%）。三个域全线下滑，telecom 这个近天花板的域从 97.4% 掉到 90.7%。
- 任务级：@499 → @999 改善 40 / 退步 76，纯抖动出现这么多改善的 P = 1.000。

### 改口：机制不是「少动手」，是「越查越多、查完做不对」

上一条我把「写调用显著下降」列为**硬结论**，还说 v4.1 是「多查、少写」。**@999 没撑住这个读法**：写调用回到了 SFT 水平、不再显著，但分数是最差的。写调用少不是病因。

四个点里真正单调、且每一点都显著的只有一条：**读调用一路涨**，5.820 → 6.425 → 6.673 → 6.728 → 6.782，相对 SFT 起点 +0.96 [+0.71, +1.25]。telecom 上同样是调用数从 16.3 涨到 19.6（+20%）。

**retail DB 也一路掉到 63.3%，相对 SFT 起点 −6.66 [−12.28, −0.88] 现在也显著变差了**（前三个点都还跨 0）。

还有一条我上一条讲反了：我说 NL_ASSERTION「一路涨到全表最高」。那是三个点时的样子，@999 它**跌回 91.7%，比 SFT 起点还低**。所以不是「拿做对换说对」——到 999 是**两头都在掉**，只是 DB 掉得更早更狠。

### telecom 掉 6.7pp，先排除了非能力因素

近天花板的域掉这么多，得先证明不是链路故障：

- 结束原因 **100% 是用户主动结束**，没有一条撞步数上限。
- **零**空 assistant 消息、**零**「一次工具都没调」的 sim。
- 工具报错确实涨了（0.007 → 0.042 次/sim），但只覆盖 **4.2%** 的 sim，解释不了 42 个失败。

**是能力退化，不是跑挂了。** 未判分 9/1112（0.81%，前几个点是 5），已单列、没填 0。

### 现在的处境

@999 这个点把话说清楚了：这个 reward 版本**训练越久越差**，而且差到了 RL 前的起点以下。剩下 5 个点（649/699/749/799/899，job 689864 / 689877 / 689880 / 689887 / 689888）还在排队，它们能补的是「什么时候开始转折」这条曲线的形状，**不会改变「999 比起点差」这个结论**。

下一步该定的是 reward 怎么改，而不是再多跑几个 checkpoint。**读调用单调膨胀、且不转化成正确动作**，是目前唯一贯穿四个点的信号，应该从这里入手。

---

## 08-09 04:40 — 奖励修正版（job 679134）的 tau3 外部验收：没长分，模型学会了多查、少动手

> ⚠️ 这条里「写调用显著下降」和「拿做对换说对」两个读法**已被 @999 推翻**，见上面 08-09 21:30。写调用在 549 触底后回升，NL_ASSERTION 到 999 也跌回起点以下。下面的三点数据本身没错，错的是我从三个点推出的方向。

前面几条看的都是训练内部的指标。这条是**第一次把这个 run 的 checkpoint 拉到 tau3 上外部判分**，499 / 549 / 599 三个点。三个域（airline / retail / telecom），banking 按约定排除。

**结论不好听：相对 SFT 起点 +1.26 / +1.17 / +0.05 个百分点，三个点的置信区间全部跨 0。**

| 模型 | 总分 | airline (50 任务) | retail (114) | telecom (114) |
|---|---|---|---|---|
| RL 前的起点 8800base | 77.5% | 63.1% | 65.1% | 96.6% |
| SFT 收官 s3000 | 78.0% | 66.5% | 66.0% | 95.0% |
| *上一版 RL v3 @499* | *82.2%* | *70.0%* | *73.5%* | *96.3%* |
| *上一版 RL v3 @599* | *82.5%* | *75.0%* | *72.1%* | *96.2%* |
| **v4.1 @499** | **79.2%** | 71.2% | 64.7% | 97.4% |
| **v4.1 @549** | **79.1%** | 73.5% | 62.1% | 98.9% |
| **v4.1 @599** | **78.0%** | 70.5% | 62.9% | 96.5% |

作为对照，上一版 RL v3 在同起点、同一套统计口径下是 **+4.54 [+2.07, +7.00]，显著**。

> 又判错一次，撤回：只有 @549 一个点的时候我说过 airline +7.00 / telecom +3.94「显著」。@599 出来两个域都回到不显著。那是单个 checkpoint 的位置，不是趋势。跟上面 240 步判据那条是同一个毛病——**一个点不能讲方向**。

### 唯一单调的东西：retail 在拿「做对」换「说对」

retail 是全有全无判分，由 **DB**（数据库真被改对）和 **NL_ASSERTION**（该说的话说到）合成。拆开看：

| 模型 | DB（事做对） | NL_ASSERTION（话说对） |
|---|---|---|
| SFT s3000 | 70.1% | 92.6% |
| *RL v3 @499* | ***77.4%*** | *93.5%* |
| v4.1 @499 | 67.8% | 94.2% |
| v4.1 @549 | 63.8% | 95.3% |
| v4.1 @599 | **64.5%** | **95.3%** |

**DB 一路掉，NL 一路涨到全表最高。** 这是整份数据里唯一三点单调、且两个方向相反的线。把退步钉死：v3 @499 → v4.1 @499 总 reward −8.77 个百分点（显著变差），其中 **DB −9.65（显著变差）、NL +0.67（不显著）**。退步 100% 来自 DB，一点不来自表达。

### 机制：多查、少动手

| 模型 | 写调用/对话 | 读调用/对话 | DB 通过率 |
|---|---|---|---|
| SFT s3000 | 1.439 | 5.820 | 70.0% |
| *RL v3 @499* | *1.414* | *6.156* | ***77.4%*** |
| v4.1 @499 | 1.336 | 6.425 | 67.8% |
| v4.1 @549 | **1.239** | 6.673 | 63.8% |
| v4.1 @599 | 1.289 | **6.728** | 64.5% |

相对 SFT 起点：v3 @499 是写 −0.02（**不显著，没少写**）/ 读 +0.34（显著）；v4.1 @599 是写 **−0.15（显著变差）**/ 读 **+0.91（显著）**。

**两条路线的差别就在这里：v3 是「多查、照写」，v4.1 是「多查、少写」**——读得比 v3 多近 3 倍，同时写动作显著减少。retail 的 DB 只认实际发生的变更，查得再多不给分；查完把发现讲清楚，恰好满足 NL_ASSERTION。那对反向曲线就是这么来的。

这跟前面 P_write 定价那条对得上：**P_write 被低估了 1.7 倍，模型现在真的在少写。**

排除掉的其它解释：不是步数截断（结束原因 100% 是用户主动结束）、不是工具报错（报错率反而从 0.221 降到 0.116）、不是转人工逃单（比 SFT 和 v3 都低）、不是不动（零调用对话占比 0）。

### 措辞上的分寸

- **硬的**：写调用显著下降、读调用显著上升，置信区间不跨 0。
- **不硬的**：DB 相对 SFT 起点 −5.5 个百分点，区间 [−12.1, +1.1] 跨 0。真正显著的是**相对 v3 的 −9.65 [−14.25, −4.82]**，别把前者说成显著。

口径：按**任务**重采样的聚类 bootstrap（同一任务 4 次重复高度相关，不能按对话采），固定种子、4000 次，与之前 SFT 阶梯和 v3 归因是同一套代码路径。自检通过——这份代码能精确复现历史上 v3 的 +4.54 [+2.07, +7.00]。未判分的对话单列不填 0（每个点 5/1112）。原始产物只读，全程没重跑、没重新判分。

**一个补不回来的不可比因素**：用户模拟器的网关换过，v3 那两列当时用的网关现在一个拒服务、一个已下线，v4.1 只能走现存的。所以 **v4.1 与 v3 的头对头夹了一层网关差异**；但 v4.1 与 SFT / base 是同网关的，上面「增益≈0」和「写调用显著下降」两条都建立在同网关比较上。

### 一个历史 bug

判断「是不是写动作」的正则漏了 `return`，于是 `return_delivered_order_items`——retail 最主要的写动作之一，占写调用约 27%——一直被算成读调用；另外 `add` 会子串命中 `address`。影响：之前 SFT 阶梯报告里「写调用下降 4.0%」那行是**系统性低估**。所有模型同样低估、方向大概率不变，但绝对水平不可信。本条改用显式白名单，没沿用那个正则。

还有一个流程漏洞：上一轮有个 checkpoint 转换起了但提交那步没接上。现改成「转好一个立刻提一个」的串行流水线，转换失败就不提交。

### 正在跑

第二批 6 个点把曲线补到 999：649 / 699 / 749 / 799 / 899（job 689864 / 689877 / 689880 / 689887 / 689888，NORMAL），**999（job 689892，HIGH，已在跑）**。加上已完成的三个点，这条曲线会有 **9 个点**。

999 要回答的问题很具体：**写调用是继续掉，还是回升。** 继续掉 = reward 的结构性问题，得改 reward；触底回升 = 训练早期的一次性代价，可以继续跑。

---

## 08-07 21:40 — 100 步判据：两个 run 还分不出来，这是预期的

新 run 到 101 步。先说能同口径比的六项——两个 run 都原生记了的字段，前 101 步池化：

| | 旧 run | 新 run | 差 |
|---|---|---|---|
| token/ep | 7590 | **7089** | **−6.6%** |
| 步数/ep | 4.26 | 4.27 | +0.2% |
| 调用/ep | 3.26 | 3.27 | +0.2% |
| reward | 0.612 | 0.598 | −2.2% |
| `R_traj` | 0.496 | 0.496 | +0.1% |
| **`R_state`** | **0.765** | **0.766** | **+0.1%** |

**除了 token 少 6.6%，分不出来。** 三个惩罚项花掉 2.2% 的 reward，换来 6.6% 的 token，`R_traj` 和 `R_state` 一分没动。

### 四条验收指标

| | 基线 | 现在（末 10 步） | |
|---|---|---|---|
| 调用/ep | gt 2.93，目标 3–4 | **3.52** | 在带内 |
| read 精度 | 47.9%，旧 run 末期 29.9% | **0.414**（首 10 步 0.353） | 在涨，仍低于基线 13.6% |
| P(≥1 多余写) | 10.9%，旧 run 末期 18.5% | **0.095**（首 10 步 0.058） | 没超基线 |
| **`R_state`** | 旧 run 同期 0.765 | **0.766** | **没动** |

read 精度这条**我没法跟旧 run 同期比**。旧 run 根本没记这个字段，要补算就得回到那条正则路径，而幻影调用会进精度的分母、把旧 run 压得偏低、反过来把新 run 衬得偏好。宁可不比，只报新 run 内部的走向。

P_write 那条之前定的触发条件是「掉到 5% 以下说明罚过头」。首 10 步 0.058 确实贴着线，现在回到 0.095，**没罚过头，不给 `p_write` 加上限**。

### 但这一切都还不算数

| 旧 run 步 | 步数/ep |
|---|---|
| 81–120 | 4.62 |
| 121–160 | 5.12 |
| 201–240 | 5.68 |
| **241–280** | **7.19** |
| 281–320 | 9.10 |
| 321–331 | **13.00** |

**旧 run 在 100 步的时候也是 4.6 步，看着完全健康。** 坡在 240 步之后。现在两个 run 打平，说明的是「还没考到」，不是「惩罚项没用」。**下一个判据点 240 步。**

### 这一段我判错了三次，都记下来

1. **step 61 跳线**——阈值挂在单步原始值上，4.04 只是 2sd 噪声，这个序列每步在 2.1–4.3 之间摆，之前已经摸到过 ≥3.6 七次。是我监控设计得不对，改成挂 5 步滚动均值了。顺带发现旧版告警是一次性 latch，真的持续突破反而会被第一次毛刺吃掉，一并修了。
2. **step 60 说「平台」**——30 步不动就叫平台，说早了，step 80 就又抬上去。
3. **step 80 说「方向不对」**——128k 桶 4.47，下一个 10 步回落到 3.22。同样是摆动。

一个序列每 10 步都能摆出一次看着像趋势的东西。**在 240 步的判据出来之前，我不再对单点做方向解读。**

### 顺手：token 到底涨没涨

涨了。新 run 自己跟自己比，94 步内 token/ep 从 6080 涨到 8144（+34%），步数从 3.82 到 4.65（+22%）。但跟旧 run 同期比 token 反而低 6–11%，步数持平。

看这个别用「用掉的比例」——那个数被 1/B 缩放过，8k 显示 0.65、128k 显示 0.05，模型完全无视预算也长这样。只看 `mean_tokens` 绝对值。另外每个预算桶每步抽到的样本数在 16–104 之间跳，池化必须按样本数加权，直接对五个桶取平均是在跟踪抽样彩票。

---



新 job 跑到 30 步，旧 job 跑到 311 步。先说改口的两件事，再说新看到的东西。

### 一、我上一条给的调用数偏高 1.7 倍

上一条那张 P_write 定价表里的「3.99 / 4.95 / 7.15 调用/ep」，是我用正则去数模型输出里的 `<function name=...>` 开标签数出来的。跟生产代码逐样本对了 1792 条之后发现这条路不能用：

| 口径 | 值 |
|---|---|
| 奖励函数实际看到的调用数 | **2.93** |
| 环境实际执行的调用数 | **2.87** |
| 我用的正则数标签 | **5.06** |

只有 35% 的样本两者相等，**65% 的样本正则偏高，从来没有偏低过**，尾巴能偏到 +48。不是 `<think>` 里的标签（那部分是 0，strip 生效了）。是模型在正文里**写出了 function 标签、但环境没执行**——语法不合规、参数缺了、或者一轮里连写好几个而框架只取第一个。

正则数的是「模型打了多少字」，生产数的是「环境跑了多少次」。**奖励吃的是后者**，所以定价必须按后者算。上面那三个数应该是 3.45 / 4.52 / 6.78。方向没变（还是失控），量级要修，**那张表里「P_write 占惩罚质量 37.1%」这个结论要重算，暂时别当依据用。**

### 二、正文探针那个补丁热改不生效

我上一条说「不用重启 job，改完下一步就生效」。错了。`reward_transform.py` 是通过 `--custom-reward-post-process-path` 在 job 启动时 import 一次的，不是每步重读。补丁在磁盘上，30 步的日志里那个字段还全是空的。

不为它重启。数据本身一条没丢，都在 rollout dump 里躺着，我随时能捞出来算——已经捞了，**760 / 814 / 758 / 844 字符，30 步平稳**，远高于上一版塌到的 204。**探针不报警。** 补丁留在文件里，下一个 run 启动时自动带上。

### 三、旧 run 的 hack 机制：散弹枪的强度和预算成正比

这是这一段真正的收获。按预算分桶看每 episode 轮数：

| 旧 run 步 | 8k | 16k | 32k | 64k | 128k |
|---|---|---|---|---|---|
| 1–10 | 3.04 | 2.47 | 2.40 | 2.79 | 2.25 |
| 95–105 | 2.79 | 3.59 | 3.43 | 4.12 | 3.64 |
| 145–155 | 3.78 | 3.76 | 4.24 | 4.42 | 4.83 |
| 245–255 | 3.22 | 4.93 | 6.91 | 6.99 | 7.63 |
| **最后 10 步** | **4.87** | **7.67** | **11.65** | 9.40 | **12.58** |

**给多少上下文，它就打多少枪。** 8k 只涨到 4.87，128k 涨到 12.58。因为 `R_traj` 是纯 recall，多打不花钱，唯一的天花板就是预算本身。同期 8k 桶的超预算率从 0 涨到 6%，说明预算终止确实在最紧那一档咬住了它。

**反过来看，这说明预算梯度这个机制是有效的**——只是它只封上限，不改激励方向，所以单靠它不够。

另一个用处：**128k 桶是领先指标**。散弹枪总是先在那里冒头，比总平均早大约 50 步。新 run 的监控就盯这一档。

### 四、311 步的 reward 涨幅，全部来自可以靠蒙拿到的那一半

| 旧 run 步 | R_state | R_traj | reward |
|---|---|---|---|
| 1 | 0.726 | 0.438 | 0.573 |
| 101 | 0.739 | 0.579 | 0.642 |
| 201 | 0.810 | 0.697 | 0.741 |
| 301 | 0.853 | 0.757 | 0.772 |
| 311 | **0.698** | **0.809** | 0.742 |

`R_traj` 从 0.438 涨到 0.809，**`R_state` 从 0.726 到 0.698，311 步纹丝不动、还略降**。

这是 reward hacking 最干净的证据形式：**总 reward 的曲线是好看的，拆开之后是空的。** 涨的全是 recall——多打几枪就能蹭到的那一半；真正代表「把任务办成了」的那一半，训练了 311 步等于没训。

### 五、新 run 前 30 步：跟旧 run 同期重合，也就是说还没考到

| 桶 | 旧 1–10 | 新 1–10 | 旧 20–29 | 新 20–29 |
|---|---|---|---|---|
| 8k | 3.04 | 3.22 | 2.52 | 2.89 |
| 32k | 2.40 | 2.61 | 3.07 | 2.83 |
| 128k | 2.25 | 2.41 | 2.99 | 2.85 |

重合。超预算率全桶 ≤1%，上下文用量跟旧 run 同期一致。**三个惩罚项既没有意外收紧任何东西，也还没被考到**——旧 run 是到 100 步之后才开始爬的。

read 精度这 30 步在 0.22–0.49 之间来回跳，没有趋势。上一条我拿「0.311 → 0.402」当回升讲，那个不该讲——30 步、这个方差量级，看不出信号。**真正的判据在 100–300 步**，也就是旧 run 崩掉的那个位置。在那之前我不会再报「回升」。

监控挂在三条线上：调用数破 4.0（旧 run 第 150 步的值）、read 精度跌破 0.28、128k 桶轮数破 6.0。

---

## 08-07 11:20 — 奖励修正版起了（job 679134），另有一件 P_write 的定价问题

> ⚠️ 下面那张 P_write 定价表里的调用数偏高 1.7 倍，见上面 16:40 那条。结论方向仍然成立，量级要按 3.45 / 4.52 / 6.78 读。

新 job **679134** 已提交，8×H800 单节点，`Queued`。这一轮**只改奖励函数**，预算梯度不动（还是 8k/16k/32k/64k/128k），save-interval 50。

为什么不把预算梯度一起改：上一个 job 就是把「删 P_length」和「上预算条件化」捆在一个 run 里，散弹枪长出来之后我们花了两轮才把归因拆干净。三个惩罚项是新的、系数是回放定的、形状刚改过，它们自己需要一个干净的观测窗口。两个 run 串行的成本可以接受。

资源上不用停旧 job：池子 h800 用了 125/160，还剩 35 卡，新旧两个 run 可以并存。而且训练是从 SFT 的 checkpoint 起的、不是从旧 run 的 step-299 起的，所以两个 run 是同一个起点的干净 A/B。

### 一件我起 job 前必须说的：P_write 被低估了

`λ_write = 0.05` 当初是当护栏定的——「多余的写 R_state 已经在管了」。但把生产代码拉到真实 rollout 上跑了一遍，它不像护栏：

| 步区间 | 调用/ep | P_dup 贡献 | P_write 贡献 | P_write 占总惩罚 |
|---|---|---|---|---|
| 98–102 | 3.99 | 0.0074 | 0.0058 | 29.5% |
| 198–202 | 4.95 | 0.0087 | 0.0086 | 35.5% |
| 284–288 | 7.15 | 0.0152 | 0.0153 | **37.1%** |

系数只有 P_dup 的三分之一，惩罚质量却跟它**打平**。原因是分母：**56.9% 的 episode 的 gt 写动作数是 0**，分母兜到 1，这一项就没有上界了。实测 `P_write` 最大值 13.0，也就是单条 episode 扣 0.65 分。

我没有临时改它，按原定 0.05 起的。理由：gt_write=0 说明这个任务本身是只读的，此时任何写都确实是不该发生的，罚满是对的；而且 tau3 上 P_write 是三项里唯一让排序**变好**的（AUC +0.0010）。

但我记下了触发条件：**如果「≥1 多余写」的比例从基线 10.9% 被压到明显低于这个数（比如掉到 5% 以下），说明它罚过头了**，那时就给 `p_write` 加个 1.0~2.0 的上限，而不是动系数。

### 补一个上次欠的数：重复读到底是好事还是坏事

上次我说「下单前把航班状态再确认一遍」这种确认行为会被 `P_dup` 打到，代价只是几百 token。把通过率补出来之后，结论跟这个担心**相反**：

| | 条数 | 占比 | 通过率 | P_dup 扣分 |
|---|---|---|---|---|
| 没有重复读 | 3692 | 93.7% | **70.0%** | 0.000 |
| 有重复读 | 249 | 6.3% | **54.2%** | 0.032 |
| └ 重复 1 次 | 175 | 4.4% | 61.7% | 0.027 |
| └ 重复 2 次 | 53 | 1.3% | 37.7% | 0.039 |
| └ 重复 ≥3 次 | 21 | 0.5% | 33.3% | 0.052 |

重复读一次，通过率低 **15.8 个百分点**，而且**随重复次数单调下降**。这是相关不是因果（难任务本身既容易让模型反复查、又容易失败），但剂量-反应关系这么整齐，罚它心里有底了。

另外：**重复调用是 RL 长出来的，不是底座带的**——底座只有 1.6% 的 episode 有重复读，上一版训完是 10.6%，涨了 6.6 倍。

### 起 job 之后盯这四条

前三条是三个惩罚项各自的直接读数，第四条是唯一真正重要的：

1. **调用数/ep** — 目标 3–4（gt 均值 2.93）。超过 5 还在爬 = 惩罚整体太轻。
2. **read 精度**（匹配上的读 / 所有读）— 基线 47.9%，上一版掉到 29.9%。**这条不回升，这次修改就是白改。**
3. **P(≥1 多余写)** — 基线 10.9%，上一版 18.5%。只要求止住增长，不要求降回去。
4. **R_state** — 上一版 288 步纹丝不动（能力涨的 +0.05 被乱写吃光）。前三条都只是它的前提。三个惩罚项都生效了、调用数也压下来了，R_state 还是不动，就得停下来重新想瓶颈在哪。

再加一条上次的教训做**副作用探针**：对用户说的正文平均字符数。上一版它从 309 掉到 204、Web Search 从 74 塌到 41.5。这三个惩罚项理论上都不碰它——**正因为理论上不碰，它才是最好的探针**。

---

## 08-07 04:30 — 新惩罚项的四项回放：三项过，第四项挂了，但挂的是尺子不是公式

300 步。这一段没动训练，全在离线验证新的惩罚项——因为 v4 把 `-0.1·P_length` 整项删了之后，`R_traj` 是纯 recall（`matched / len(gt)`），多打的调用一分钱不花，散弹枪在数学上是严格占优策略。步数从 100 步的 3.5 调用/episode 爬到 290 步的 7.2，就是这个洞。

补的形状是三块分开罚，各管一件事，一件都不许兼职：

| 项 | 管什么 | 系数 |
|---|---|---|
| `P_dup` | 同名**同参**打第二遍 = 无歧义浪费 | 0.15 |
| `P_churn` | 同名**不同参**超支 = 原地打转 | 0.15 |
| `P_write` | 写动作超出 gt = 污染环境状态 | 0.05 |

全新的探索性读**一个字不罚**。它只占超额读的 20.8%，而它正是上一版在 tau3 上唯一扎实的收益（动手前多读几次 → 写动作 missing 16.9%→12.2%）。为了两成的量去砍那条，不划算。

`λ_churn` 定 0.15 而不是 0.05，因为细分跑出来同名不同参**不是翻页**：只有 3.2% 的样本只改了分页键，78.5% 来自被调 ≥4 次的少数几个工具，单工具最高被打了 117 次。这是原地打转，不是检索。

### 四项回放（语料：4500 条带逐动作判定的 tau3 sim，全离线，没重跑任何评测）

1. **不许误伤探索** — 上一版通过的轨迹，新公式给的分不能低于 base 的同 task 轨迹。**过**：+0.094 / +0.099，而且两边被扣的罚分一样（0.013 vs 0.013），说明罚的是浪费不是探索。
2. **散弹枪必须被明显压低** — **过**：健康窗口（100 步）平均扣 0.020，散弹枪窗口（286 步）扣 0.041，2.1 倍；≥10 调用的 episode 本身扣 0.124，是健康窗口的 6 倍。不是常数偏移。
3. **"想清楚了没落地"不能给高分** — **过**：172 条零写入样本，实测通过率 2.3%，新分数落在第 9 百分位。
4. **全分布排序一致性（Spearman）不能比旧公式低** — **按字面挂了**：ρ 0.7554 → 0.7022。

### 第 4 项为什么挂，以及为什么这个挂法不算数

先按预案排查，两个发现：

- 挂的**全部**是 `P_churn`：单独上 `P_dup` 是 −0.0013，单独上 `P_write` 是 **+0.0011**（帮忙），单独上 `P_churn` 是 −0.0528。
- 预案说"挂了就把 λ_churn 砍半重测"。砍半到 0.075 **还是挂**（−0.0265）。所以预案里的补救路径本身也不成立。

`P_churn` 的病根是分母：它除以 gt 的读次数，而 tau3 的逐动作清单只列**判分器要验的读**（均值 1.38），不是任务实际需要的读。一次同名重复除以 1，直接就是 1.0。这正是当初否掉 `P_read` 的那个 gt 基准偏差，从 `P_churn` 里溜进来了。改成自归一化 + 免费额度 2 次之后（`max(0, sn−2) / n_read_pred`，不碰 gt），ρ 回到 0.7505，仍然差 0.005。

**然后我去查了 Spearman 本身，这一项是这段最重要的发现**：tau3 的 reward 是二值的，旧公式又很粗（`R_state` 基本只有 0 和 1），结果 **3.97% 的"通过 vs 失败"配对是完全同分**。Spearman 用平均秩处理同分，会把每个同分块压成一个值、缩小秩方差，**把 ρ 抬高**。任何打破这些同分的项——哪怕打破得完全正确——都会失去这份虚高。

拿一列**跟结果完全无关的纯噪声**（sim 标识的哈希）当预测量验证：

```
预测量                        Spearman ρ      AUC     同分率
旧公式                          0.7554      0.9535    3.97%
旧公式 − 纯噪声                 0.7256      0.9531    0.00%
```

纯噪声一分钱信息没有，ρ 掉了 0.030，AUC 只动 0.0004。也就是说预案里那个 ρ 门槛，**罚的是"打破同分"这件事本身**，最多能白扣 0.030——而观测到的失败总共只有 0.053。这把尺子量不了要量的东西。

对二值结果，该用的统计量是 AUC（= P(通过分 > 失败分) + 0.5·P(同分)）。同一份语料、同一批公式，换成 AUC：

```
P_churn 形状                    λ      ρ       ΔAUC    判定
不加 churn 项                  —    0.7543   +0.0048   过
gt 分母（原方案）             0.15   0.7022   −0.0200   挂
gt 分母 + 免费额度 2          0.15   0.7308   −0.0052   挂
自归一化 sn/n_read            0.10   0.7471   +0.0091   过
自归一化 + 免费额度 2         0.15   0.7505   +0.0083   过
```

**结论**：原方案的 gt 分母形状，在正确的尺子下也真的挂（−0.0200），不是尺子的锅；换成自归一化 + 免费额度之后，排序一致性比旧公式**更好**（+0.0083）。所有 ρ 列全是负的，包括排序确实变好的那几行——这就是同分假象。

### 一笔已知的代价

上一版 tau3 通过的样本里，有 135 条（5.0%）会被 `P_dup` 扣到，均值 0.027、最坏 0.075。这些是"下单前把状态再确认一遍"的重复读。这刀我知道它切到什么，代价是几百 token。以后如果看见确认行为消失，是这里砍的，不是退化。

### 卡在这里，等一个决定

`P_churn` 的形状要从锁定的 gt 分母版改成自归一化 + 免费额度版，这是改了已经定死的框架，我不自己动。另外预算梯度那个决定（要不要停掉重开、换低一档的预算梯度）还挂着，起新 job 跟它冲突。两件一起定了再起。

---

## 08-07 00:40 — 步数抬了一档但没失控；我中间判错两次，都记在这

190 步。这一段我对步数的判断反复了两次，先把过程写清楚，因为结论本身没有过程重要。

每 10 步一个采样点的轮数：

```
step   50   60   70   80   90  100  110  120  130  140  150  160  170  180  190
turns 4.1  3.8  4.2  4.1  4.6  3.7  4.2  4.6  4.9  6.2  7.2  4.9  5.7  7.7  5.5
```

- step 100 我说「饱和了」。依据是窗口均值 4.46→4.62→4.62 横着走。**说重了** —— 同一份数据的后 50 步 OLS 是 +0.85 轮/100 步，不是零。
- step 150 我说「确认失控，绊线约 step 157 触发」。依据是 6.2、7.2 连续两点。**说错了** —— step 160 掉回 4.9，绊线至今没响。两点连升就下结论，判早了。

积够 6 个点之后的稳当说法：

```
step  50-130 (9 点)   4.24 ± 0.13
step 140-190 (6 点)   6.20 ± 0.46
差    +1.96 ± 0.48  →  4.1σ
```

**是水平位移 + 方差放大，不是单调失控。** 均值抬了约 2 轮，波动幅度大约是原来的 2.5 倍。这类单步均值本来就重尾（分布 p99=20、max=24），少数几条长 episode 就能把一步顶到 7.7。

一个副作用：**绊线阈值失效了。** 设在 8 的时候，7.2、7.7 都还是正常波动的上沿，单点阈值现在只会误报。等能连上要改成「连续两次 ≥7」这种形式。

### 更该担心的是另一条

`mixed`（32 组里有几组的 8 条 rollout 分数不全同、能产生梯度）：

```
window  0-9  40-49  80-89  90-99      step 170  step 190
mixed  25.6   25.7   23.0   22.1            20        18
```

从 80% 掉到 56%。配上 step 100 那个发现——涨的全是 `R_traj`（+24%），`R_state` 一百步没动——指向同一件事：**模型在把已经会做的做得更漂亮，没在攻没做成的。** 简单任务被吃透（`all1` 从 2.9 翻到 6.8），那些组不再产生梯度，有效 batch 在缩水。

这条比步数值得盯。步数抬 2 轮不致命，梯度源枯竭会让后面 1800 步白跑。

### 卡住的事

跳板机登录态过期，新连接全部要浏览器重认证，**所有临时分析都做不了**。已经积压两件必须看逐 episode 数据才能定性的：轮数抬升是整体变长还是尾巴变肥（处理方式完全不同）、以及 `mixed` 下滑是不是集中在特定任务类型。

训练本身正常，job Running，checkpoint 每 50 步落盘，最新 `iter_0000149`。

阶梯的决定（step 100 那条列的三个选项）仍然待定。这次步数抬升不构成新的重起理由 —— 理由还是原来那个：五档里三档从来没被预算碰过。

---

## 08-06 22:55 — step 100 判定：三条全部答完，结论不好听

事先说好在 step 100 带三件事回来。都算完了。

### 一、条件化：**未出现**，而且现在能给出比"没到门槛"更强的话

按事先定死的门槛（64k−32k 和 128k−32k 同时为正且 ≥2σ，连续三个不重叠的 10 步窗口）：

```
window        32k     64k    128k |  64k-32k   z  | 128k-32k   z
  0-9        5919    6518    6773 |    +599  +1.10|    +854  +1.49
 10-19       6363    6022    6102 |    -341  -0.56|    -261  -0.40
 20-29       6774    6739    6929 |     -35  -0.05|    +155  +0.26
 30-39       6769    8163    8345 |   +1394  +2.72|   +1576  +2.43   ← 唯一合格的一窗
 40-49       7519    7427    8332 |     -93  -0.20|    +813  +1.17
 50-59       9138    9138    8234 |      +0  +0.00|    -904  -1.33
 60-69       8771    9572    8421 |    +801  +0.86|    -350  -0.55
 70-79       8742    8181    9395 |    -561  -0.93|    +653  +0.75
 80-89       9955    9300    9849 |    -655  -1.19|    -107  -0.16
 90-99       9799    9827    8574 |     +28  +0.03|   -1225  -1.56
```

连续合格窗口数 = 1，**判定：未达成**。

但 10 个窗口攒够了样本，可以直接给整段的估计，这比"没到门槛"信息量大得多：

- `64k − 32k` = **+114 ± 202**（z=0.56）
- `128k − 32k` = **+120 ± 273**（z=0.44）

不是"还没看出来"，是**测出来就是零**，而且误差棒已经收到 ±200 左右。100 步、约 25000 条 episode 之后，上带三档彼此无法区分。符号在 10 个窗口里翻来覆去（+ − − + − 0 + − − +），是纯噪声的样子。

### 二、步数漂移：**饱和了**

```
window      轮数    ±     tokens
  0-9      3.59  0.16     5933
 30-39     4.46  0.22     7265
 60-69     4.62  0.20     8546
 90-99     4.62  0.12     8599
```

前 30 步涨了 0.87，之后 70 步只涨 0.16，在 4.2–4.6 之间横着走。OLS 斜率从前 50 步的 +0.0185 掉到后 50 步的 +0.0085，减半。`n_hard_step_cap` 全程 0。**之前担心的轮数爆炸没有发生**，绊线（≥8）也没响过。

但有个换了形态的东西：**每轮的长度在涨。** 5933/3.59 = 1652 → 8599/4.62 = 1861 token/轮，+13%。轮数停了，单轮写得更长了。总量还在涨，只是换了个地方涨。

### 三、32k 以上是否仍然零超预算：**是，十个窗口全是硬零**

```
window       8k      16k      32k      64k     128k
  0-9    0.0020   0.0000   0.0000   0.0000   0.0000
 40-49   0.0301   0.0053   0.0000   0.0000   0.0000
 70-79   0.0286   0.0107   0.0000   0.0000   0.0000
 90-99   0.0250   0.0051   0.0000   0.0000   0.0000
```

8k 在 2.5–3.75% 附近也平了，16k 稳定在 0.5–1%。**32k / 64k / 128k：一百步、零次。**

### 我上一条的预测是错的，撤回

上一条我说"通胀若持续，32k 迟早自己咬住，反而不用改阶梯"，并说 step 100 用实测斜率重算。算完了：**不会。**

token 增速在减速 —— 前 50 步 +28/步，后 50 步 +11/步。按后半段的斜率，均值从 8.6k 爬到 32k 还要 **2200 步**，比整个 run（2000 步）还长。这条路走不通。

### 所以现在的处境

这个实验设计上有个硬伤，现在数据说清楚了：**五档里有三档从来没被预算碰过一下。** 那 60% 的采样量对"预算条件化"这个课题贡献是零 —— 它们就是普通 RL。剩下 40% 里，只有 8k 那档真在承压，而它承压的方式是 2.5% 的 episode 被判零，同时伴随删失偏差。

上带三档测不出差异，最自然的解释不是"模型学得慢"，是**那三档根本没有可学的东西**：用 8.6k 去填 32k / 64k / 128k，剩余预算字段从头到尾显示"还剩一大半"，三种情况下的最优策略完全一样。模型没理由区分，也确实没区分。

### 要不要把阶梯下移 —— 这是需要决定的事

训练数据的 prompt 长度分布：p50=4099，p75=5883，p90=8323，p95=10020，p99=15248，max=22469。当前 episode 均值约 8.6k。

几个候选（括号内是「有多少比例的行够资格被分到这一档」）：

```
现行     8k(0.77)  16k(0.99)  32k(1.00)  64k(1.00)  128k(1.00)   ← 只有 1 档 ≤12k
方案 A   4k(0.15)   8k(0.77)  16k(0.99)  32k(1.00)  64k(1.00)   ← 4k 档只有 15% 行够资格，太挤
方案 B   6k(0.50)   8k(0.77)  12k(0.95)  16k(0.99)  24k(1.00)   ← 三档会真的咬住
方案 C   6k(0.50)  10k(0.89)  16k(0.99)  32k(1.00)  64k(1.00)   ← 折中，保留一个宽档做对照
```

我倾向 **B 或 C**。B 让五档里有三档承压，实验才有信噪比；C 保留 32k/64k 作为"不受约束"的对照组，代价是只有两档在学。A 不行 —— 4k 档只有 15% 的数据够资格，会把那一档退化成"只喂最短的任务"，把已经很麻烦的资格混淆放大一倍。

代价：**改阶梯要重启。** 已经跑了 100 步、约 3.2 小时。可以从 `iter_0000099` 续跑，但模型已经对旧阶梯适应了 100 步，续跑的曲线会混着两套预算的历史，不好读。要干净就从头起，2000 步还是约 3 天。

**这个我不自己拍，等你定。** 三个选项：① 什么都不改，接受这个 run 测的是别的东西（模型会变强，但"预算条件化"这个问题不会有答案）；② 从头用 B 或 C 重起；③ 从 `iter_0000099` 续跑 + 换阶梯，省 3 小时但读数会脏。

### 附：一个顺带发现的问题

奖励是 `0.5·R_traj + 0.5·R_state`。拆开看：

```
window   reward   r_traj   r_state   mixed/32   all1
  0-9     0.571    0.428     0.758      25.6     2.9
 40-49    0.633    0.509     0.791      25.7     4.2
 80-89    0.675    0.568     0.800      23.0     7.1
 90-99    0.617    0.530     0.742      22.1     6.8
```

**涨的全是 `R_traj`（0.428→0.530，+24%），`R_state` 一百步基本没动**（0.758→0.742，在噪声里）。R_state 才是"环境最后有没有到正确状态"，也就是任务真的做成没做成。所以这一百步模型学会的主要是把轨迹写得更像样，不是把事办成。

同时组内多样性在退化：`mixed` 从 25.6 掉到 22.1，`all1`（八条 rollout 全对、不产生梯度）从 2.9 涨到 6.8，翻了一倍多。简单任务在被吃透。目前 69% 的组还有梯度，不到要动手的程度，但趋势是单调的，一起盯着。

---

## 08-06 21:40 — 第二个判据也被污染了，方向和我说的相反；70 步

我上一条把 `res8`（8k 档相对资格基线的残差）当作干净判据，说它转正意味着模型开始在预算咬人的地方省着用。**拆开绝对值之后这个解读站不住。**

各档绝对 token 消耗：

```
window        8k          16k          32k          64k         128k
0-14      5055±133    5229±333    6243±329    6209±256    6363±258
15-29     5142±192    6423±292    6460±353    6644±389    6839±383
30-44     5621±240    7028±366    7028±366    7789±344    8071±496
45-61     5602±231    7888±342    8434±415    8707±374    8294±399
```

**8k 的绝对消耗是在涨的，不是在降**：5055 → 5602，+548 ± 266（2.1σ）。res8 转正纯粹因为别的档涨得更快 —— 16k +2659、32k +2191、64k +2498、128k +1931。

全线通胀，8k 只是涨得慢。而它涨得慢很可能不是"学会节制"，是**删失效应**：超预算的 episode 被当场终止，观测到的 token 均值被机械压低。这是选择偏差，不是策略。

超预算率的走势支持这个解释：

```
window       8k      16k      32k      64k     128k   全局
0-14      0.003    0.001    0.000    0.000    0.000   0.001
15-29     0.014    0.000    0.000    0.000    0.000   0.002
30-44     0.008    0.004    0.000    0.000    0.000   0.002
45-59     0.045    0.003    0.000    0.000    0.000   0.007
60-70     0.030    0.005    0.000    0.000    0.000   0.006
```

8k 从 0.3‰ 涨到 3–4.5%，涨了一个数量级；16k 开始有了；**32k 以上仍然是硬零**。

所以三个判据里已经废掉两个。**`band`（32k/64k/128k 的极差）是唯一还干净的** —— 那三档零超预算、无删失，任务资格也完全相同。它现在是 650~，无序。**条件化仍然没有证据。**

### 真正在发生的事

模型在变强，也在变长。reward 0.61→0.67，`r_traj` 在涨，轮数 3.69→4.00→4.47→4.23，token 全线涨 30–50%。这是 RL 的常见形状：多调工具能拿分，于是越写越长。预算目前只在最紧的一档开始反推。

一个没预料到的后果：**这反而降低了"下移阶梯"的必要性。** 如果通胀继续，32k 迟早自己咬住，不用改配置。按当前斜率（每 15 步约 +1500 token）粗估还要 100+ 步。step 100 用实测斜率重算再判断。

---

## 08-06 20:25 — 第一个 checkpoint 落盘，50 步

`iter_0000049`，4.7 GB，17 个文件（16 个 distcp 分片 = 8 卡 × 2，加 `common.pt`），完整。save-interval=50 生效。按 40 个 checkpoint 算共约 188 GB，文件系统剩 117 TB，不是问题。

平均 114 秒/步，**2000 步约 3 天**。

### 两个判据：方向对，但没到门槛

```
残差(vs 8k)   step 0-11    16k:-1041  32k:-389  64k:-347  128k:+150
             step 36-47    16k: +547  32k:+238  64k:+404  128k:+1614   ← 首次四档全正

上带          step 0-11    32k:6188  64k:6230  128k:6727   极差  539
             step 12-23    32k:6445  64k:6252  128k:6209   极差  236
             step 24-35    32k:6610  64k:7628  128k:7344   极差 1018
             step 36-47    32k:7501  64k:7666  128k:8876   极差 1376
```

事先定死的门槛是「64k/128k 对 32k 连续三个窗口保持正且 ≥2σ」。实际最后一窗 `64k−32k` 只有 +165 —— 极差是被 128k 单独拉起来的，不是三档整齐分开。**按事先的标准判定为"未出现"**，不事后挪门槛。

reward 在 0.6 附近，step 40 那个 0.715 是尖峰不是台阶。

### 步数在漂，这是目前最扎实的一个信号

删掉 `max_steps` 之后，每条 episode 的轮数从 3.59 涨到 4.33，**+0.73 ± 0.20，3.6σ**。没有爆炸（分布 p50=4 / p90=8 / p99=20 / max=24，500 的硬上限一次没碰过），但趋势是实的。

结构问题在于：**"由预算隐式约束轮数"只在预算咬得住的地方成立。** 按每轮约 1700 token 算，128k 预算要跑到**约 77 轮**才判零。32k/64k/128k 三档实测超预算次数为 0，占 66% 的采样量 —— 这些样本当前的实际轮数上界就是 77，不是任何人设想的数字。真跑到 77 轮，后 70 轮全是垃圾，还占着 GRPO 的一个名额。

墙钟代价倒是没有想象中大：单步中位时间 83s → 108s，涨 30%，`corr(轮数, 秒数) = −0.20`。所以这件事该担心的是轨迹质量，不是训练速度。

已挂绊线：`mean_steps ≥ 8` 立即报警，不等 10 步整点。

---

## 08-06 20:10 — 我之前给的判据是错的，改了

先说结论：**「各档 token 消耗随预算单调排开」这个判据不成立，不能用它来判断模型学没学会。** 我前面两次都是这么说的，作废。

跑到 31 步时，五档的 token 消耗确实排成了单调递增，秩相关 +1.00、p=0.008，看着像信号。但它是假的，来源在采样器里：预算不是随便采的，只在**满足 `B ≥ prompt_len + 2048` 的档位里**均匀采 —— 长 prompt 根本不可能被分到小预算。这是当初有意的设计（预算该是个挑战，不是不可能完成的任务），但副作用是**档位和任务长度绑死了**。

2790 条训练数据里，**23.1% 永远进不了 8k 档**，而且正好是最长的那 23%；16k 档也排掉 1.3%。所以 8k 档天生被喂更短的任务，就算模型完全无视预算字段，它的 token 数也会更低。

把这份"光靠资格就能预测出来的差距"减掉之后，剩下的才是模型能负责的部分：

| 档位 | 实测差距(vs 8k) | 资格预测差距 | 残差 |
| --- | --- | --- | --- |
| 16k | +892 | +1285 | −393 |
| 32k | +1299 | +1539 | −240 |
| 64k | +1371 | +1539 | −168 |
| 128k | +1559 | +1539 | **+20** |

残差基本是 0。**那个单调排列 100% 是采样器造出来的，模型侧一点条件化信号都没有。**

**换成两个不受污染的判据：**

1. **残差**（上表那一列）随训练往正走 —— 意味着 8k 档在相对收缩，模型在预算真正咬人的地方开始省了。
2. **上带对比（32k / 64k / 128k）**。这三档 100% 的任务都够资格，期望任务长度完全相同，所以它们之间的任何差异**天生无污染**，不需要任何校正模型。这是整个 run 里最干净的一个测量。

目前两个都还没动静：

```
残差    step 0-7   16k:-1250  32k:-472  64k:-451  128k:+399
       step 24-31  16k: -514  32k:-286  64k:+372  128k:+404

上带    step 0-7    32k:6086  64k:6107  128k:6957   极差 871
       step 8-15   32k:6774  64k:6204  128k:5901   极差 873   ← 顺序还反了
       step 24-31  32k:6755  64k:7414  128k:7446   极差 691
```

残差在 −1250 到 +404 之间乱跳没有趋势，上带极差在 488–873 之间平着、顺序还翻转过一次。34 步，本来也太早。

一个诚实的限制：上带这三档，模型现在只用到 32k 里的 6k 左右，离上限远得很，本来就没什么压力去节制。所以**上带没差异是弱证据，有差异才是强证据**。真正会先动的应该是 8k 档的残差。

---

## 08-06 19:30 — 跑起来了，13 步

**job 675802，Running。** 之前那版（675737）在 train step 0 就崩了，报 `tensor a (17) must match tensor b (9)`。原因是 CP=4 之后有些分片一个 loss token 都没有，logdiff 那段指标是有条件加 8 个 key 的，于是不同分片上报的 key 数量不一样，聚合时对不上。上一版 v3 没事纯粹因为它是 CP=1。关掉 logdiff 指标就好了，代价是少了一个训练/rollout 一致性的诊断项。

跑到 step 12：

| step | 平均 reward | 无梯度组 | 超预算率 | R_traj | R_state |
| --- | --- | --- | --- | --- | --- |
| 9 | 0.579 | 4/32 | 0.000 | 0.463 | 0.742 |
| 10 | 0.512 | 5/32 | 0.000 | 0.369 | 0.665 |
| 11 | 0.607 | 0/32 | 0.000 | 0.461 | 0.772 |
| 12 | 0.622 | 4/32 | 0.000 | 0.457 | 0.884 |

四个守卫计数器全程 0：预算不可行的配对、硬步数上限、被截断的样本、缺预算元数据，一个都没有。组内有差异的比例稳定在 22–30/32，梯度是有的，而且没靠 dynamic sampling 过滤撑着。

**机制是活的，不是死代码。** 前 143 条 episode 里抓到 1 条 `budget_exceeded`，硬清零那条路径真的会触发。step 4 第一次咬住，只有 8k 那档在超，超出率 2%，其余四档全 0 —— 这正是想要的形状：只对最紧的一档生效，宽档不受影响，说明预算不是在无差别地砍 episode。

**但现在还读不出条件化。** 各档的绝对 token 消耗（这才是关键指标）：

> ⚠️ 这一段的判据后来被推翻了，见上面 20:10 那条。往下读的时候当历史记录看，别当结论。

```
8k: 4748    16k: 7845    32k: 7049    64k: 4822    128k: 6232
```

五个数字挤在 4.7k–7.8k，跟预算完全不相关，最紧的 8k 甚至不是用得最少的。要看的是这五个数字**单调按 B 排开**，那才叫学会了条件化。13 步远远不够，每档每步才 40–60 个样本，这个抖动量级完全在噪声里。

顺带说一个容易看错的地方：**不要看「用掉的比例」**。那个数现在是 0.60 / 0.34 / 0.19 / 0.10 / 0.05，看着像「预算越紧用得越省」，其实是被 1/B 这个因子做出来的假象 —— 分母不一样，模型完全无视预算也会长这样。

**一个要盯的混淆项**：8.4% 的 episode 是 `truncated` 结束的，这条路径超预算标记是 0，不走判零，是单轮输出长度撞到上限。目前可接受，但如果模型为了省预算改成写更长的单轮输出，这个比例会涨，并且会和预算实验纠缠在一起。

checkpoint 每 50 步存一次，第一个在 step 50。

---

## 怎么看这个仓库

- 这个文件（RL_V4.md）—— 当前在跑的 RL 训练，人写的
- [PROGRESS.md](PROGRESS.md) —— 之前的评测线（Toolathlon / MCPMark），人写的
- [STATUS.md](STATUS.md) —— **已停止刷新**，原来是每 10 分钟机器自动 commit 的，现在关掉了，下面的数是 08-06 19:18 的快照
