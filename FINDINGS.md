> **2026-08-06 最终结论（推翻了当天早上那条更正）**：泄漏**确实是 SGLang 解析器的 bug**，而且已定位到具体一行、已修复、已验证。当天早上我曾把它改判为「模型输出非法 XML」，那次改判用错了方法——我拿解析后的 content 当模型原始输出看。正确的判据是：**同一段文本，单独喂进解析器失败，前面加一个合法块就成功**。详见文末「2026-08-06 下午：定位到解析器的未初始化变量」。本文正文关于「36% 的任务被 bug 毁掉、分数不能当测量结果」的结论**成立**。

# 关键发现：0 分数据被两个我方问题污染

**2026-08-05 10:45（更正了上下文长度的结论）**

两个污染源：**工具调用解析故障**（毁掉 36% 的任务）和**服务只开了 64k 上下文**（模型实际 128k，影响另外 27%）。两个都是我这边可修的，不是模型的问题。

## 结论

昨晚那批 0 分**不能当作模型的真实水平**。Toolathlon 108 个任务里有 **36 个（33%）是被工具调用解析失败直接打断的**，不是模型自己做错或做完。

## 现象

模型输出的工具调用是 MiniCPM 风格的 XML，正常情况下应该被服务端解析成 `tool_calls` 对象。但在相当一部分回合里，这段 XML 原封不动地留在了消息正文里：

```
<function name="local_terminal_run_command"><param name="command">ls -la /workspace/dumps/workspace</param></function>
```

一旦发生，这一步的工具就没有执行，agent 循环也就到此为止。

## 量化

| 指标 | 数值 |
| --- | --- |
| 出现过 XML 泄漏的任务 | 39 / 108 |
| 泄漏发生在最后一条消息（即回合中断于此） | 36 |
| 泄漏的消息条数 | 46 |
| 成功解析的消息条数 | 1553 |
| MCPMark filesystem 受影响任务 | 4 / 30 |

单条消息的失败率只有约 3%，但因为**一次失败就终止整个任务**，最终毁掉了三分之一的题。

## 排查进展

**已排除：环境和评分器有问题。** 同一套 harness、同一台机器上，kimi-k3 的历史记录是 108 题通过 77-81 个（71-75%）。评分链路没问题。

**已排除：流式解析。** 曾怀疑是流式增量解析的锅，但 Toolathlon 的客户端是 `stream=False`，而且我那个离线流式测试连已知能成功的对照样本都判成失败，测法本身不可信，结论作废。

**已确认：解析器对部分输入确实处理不了。** 把 46 条泄漏原文喂给 SGLang 的 `minicpm4_xml` 解析器离线重放，**10 条即使在非流式下也解析不出来**，其中有解析器抛 `Object of type ellipsis is not JSON serializable` 的情况。这部分是解析器的硬缺陷。

**待验证：剩下 36 条。** 这些离线重放能正常解析，但线上漏了。差异很可能在工具 schema——离线重放时我给的是宽松 schema（不限定参数），而真实请求带的是有类型约束的 schema，解析器要按 schema 做参数校验和类型转换，失败就吐不出调用。轨迹里没有保存原始 schema，所以要等 GPU 服务恢复后用真实 schema 在线复现。

## 泄漏消息的特征分布

46 条里：参数含换行 17、含 `<![CDATA[...]]>` 6、一条消息里多个 `<function>` 6、同条带 `<think>` 标签 4、XML 前面还有其他文字 4。没有单一主因。

## 另一个独立问题（更正：这是我的部署失误）

29 个任务（27%）中途撞到 65536 上下文上限。**这堵墙是我自己砌的，不是模型的限制。**

**已证实**：昨天 8 个 SGLang 副本的日志里全是 `context_len=65536`。这是因为我在命令行显式传了 `--context-length 65536`，值取自 checkpoint 的 `config.json`（当时该字段写的是 65536）。

但模型实际是按 **128k** 训练的：

- `/user/zhouyiran/patch_mpe.py` 就是专门把这个字段从 65536 改成 131072 的脚本，说明这是已知问题；
- `rope_theta = 5,000,000`，这个量级对应长上下文训练；
- 同项目目录下有 `longsft_512k` 字样的历史评测产物。

目前 10 个相关 checkpoint 的 config 都已是 131072（`config.json.bak_mpe` 里留着原值 65536）。注意这个字段**曾被还原过一次**——昨天部署前一分钟 patch 过，今早读到的又是 65536——所以部署脚本里除了显式传 131072，还保留 `SGLANG_ALLOW_OVERWRITE_LONGER_CONTEXT_LEN=1` 作为保险。

所以这 27% 的任务是被部署配置腰斩的，和解析故障一样属于**可修复的污染源**，不是模型的结构性劣势。

容量上没有障碍：昨天每个副本的 `max_total_num_tokens` 约 145 万，128k 的序列放得下。

---

# 与 kimi-k3（71-75%）环境逐项对比

## 配置层面：完全一致，没有差异

| 对比项 | 结论 |
| --- | --- |
| `e2b.env` 凭证 | 13 个 key 完全相同，只有模型地址/密钥/模型名不同。**不存在我方缺少 SaaS 凭证的情况** |
| 沙箱镜像 | 同一个 `toolathlon-task-image:1016beta-k3s` |
| E2B 模板与域名 | 同一个 `openclaw-new` / `ap-beijing.tencentags.com` |
| 任务参数 | `max_steps=100`、`max_turns=50`、`single_turn_mode=true`、工具集完全一致 |
| 并行度 | 都是 workers=6 |

唯一的链路差异：他们走外部 API（`ai.ctaigw.cn`）直接返回结构化 `tool_calls`；我们走自建 SGLang + `minicpm4_xml` 解析器。

## 行为层面：三个数量级差异

| 指标 | 我方 | kimi-k3 |
| --- | --- | --- |
| XML 泄漏任务数 | **39 / 108** | **0 / 107** |
| 工具调用中位数 | 10 | 26 |
| 对话轮数中位数 | 12 | 18 |
| 输出 token 中位数 | 4706 | 10006 |

## 第二个独立问题：服务只开了 64k，而模型有 128k

峰值输入 token 分布：

| | p50 | p90 | 最大 | 超 60k 的任务数 |
| --- | --- | --- | --- | --- |
| kimi-k3 | 60433 | 172250 | 494588 | **54 / 107** |
| 我方 | 26612 | 47510 | 55912 | 0 |

**kimi-k3 有一半任务的峰值上下文超过 60k，最长用到 49 万 token。** 我方曲线在 55912 就到顶——不是不需要更多，是撞墙后被迫压缩重置。

更正：**这个 64k 是我部署时设错的，模型本身是 128k。** 详见上一节。Toolathlon 在官方设定下默认被测模型是长上下文的，把窗口开到 128k 后这一项劣势应当基本消失，需要重跑才知道实际影响多大。

## 剔除两个干扰后的真实水平

把 108 个任务按受影响情况分类：

| 分类 | 任务数 |
| --- | --- |
| 只被 XML 泄漏影响 | 28 |
| 只被上下文超限影响 | 18 |
| 两者都有 | 11 |
| **干净（两个问题都没有）** | **51** |

在这 51 个干净任务上：

- **kimi-k3 通过 41 个（80.4%）**
- **我方通过 0 个**

失败原因都是具体的内容缺失——首作者没填、输出文件没生成、目录结构不符、邮件没发出、学生成绩没更新。

## 综合结论

1. **解析故障是真实的、必须修的**，它污染了 36% 的任务，当前分数不能作为测量结果。
2. **另外 27% 的任务是被我的部署配置腰斩的**——服务只开了 64k，模型其实有 128k。这一项改配置就能解决。
3. **但即使在完全干净的 51 个任务上，模型仍然是 0 分，而 kimi-k3 是 80.4%。** 所以修完解析器重跑，分数大概率仍然很低——修复的意义在于让测量有效，而不在于提分。

## 下一步

1. **重新部署时把上下文开到 131072**。已改好 `serve_envfactory.sh`：`--context-length 131072`，同时导出 `SGLANG_ALLOW_OVERWRITE_LONGER_CONTEXT_LEN=1`（否则 SGLang 会因为超过 config 声明的 65536 而拒绝启动）。
2. **验证 128k 真的能用**。上一条只是让 SGLang 接受这个长度，不等于权重在 12 万 token 处还能正常检索。已写好 `verify_long_context.py`，在 4k/32k/64k/96k/126k 五个长度、三个深度做大海捞针，服务起来后先跑这个再跑评测。
3. GPU 服务恢复后，用真实工具 schema 在线复现剩下 36 条的失败条件。
4. 针对性修复解析器（至少要能处理 CDATA 和触发 ellipsis 报错的那类输入）。
5. 两项都修完再重跑，届时的分数才是有效测量。
4. 如果目标是拿到有区分度的信号，Toolathlon 对这个尺寸的模型可能过难，MCPMark 或更细粒度的任务集更合适。

---

# 2026-08-06：泄漏的真正原因是模型输出非法 XML

在新机器（`673016`，2×H800，上下文 131072）上重跑 Toolathlon 后，对泄漏消息逐条核对。前 6 个判分任务里有 3 个出现泄漏，结论如下。

| 任务 | 工具名真实存在 | XML 格式 | 含 CDATA |
| --- | --- | --- | --- |
| ab-testing | **否**（`bigquery_search_files`，该组只有 `list_datasets`/`get_dataset_info`/`run_query`/`list_jobs`） | 合法 | 否 |
| academic-pdf-report | 是 | **非法**：`<param>` 开 1 闭 0 | 是 |
| apply-phd-email | 否 | **非法**：`<param>` 开 6 闭 5 | 是 |

最有说明力的是 `academic-pdf-report`：

```xml
<function name="terminal_run_command"><param name="command"><![CDATA[cd /workspace/dumps/workspace/arxiv_local_storage && ls]]></function>
```

`<param>` 始终未闭合，CDATA 之后直接跳到 `</function>`。而同一任务里 `terminal_run_command` 被成功调用过（参数 `{"command": "ls"}`），所以排除了「工具不存在」和「解析器不认这个名字」两种解释——是这一次的模型输出坏了。

**两个失败模式，都在模型侧：**

1. **编造不存在的工具名** —— 解析器按 schema 校验后拒绝，行为正确。
2. **漏写 `</param>` 闭合标签** —— 两次漏写都伴随 CDATA 块。样本仅 3 条，CDATA 的相关性目前只算线索；昨天 46 条泄漏里有 6 条含 CDATA，值得用大样本复核。

**对结论的影响**：原先「解析故障污染了 36% 的任务、分数不能当测量结果」不成立。那些任务是被模型自己的非法工具调用终止的，属于真实能力缺陷。所以 8-04 那轮 0/108 比原判断更接近真实水平。

**仍然值得做的两件事**（是缓解而非修 bug）：

- 训练侧：让模型在输出 CDATA 时可靠闭合 `</param>`；抑制工具名幻觉。
- 推理侧：给解析器加容错，对未闭合的 `<param>` 做恢复。这能救回一部分任务，但不该用来掩盖模型的输出质量问题。

---

# 2026-08-06 下午：定位到解析器的未初始化变量

## 一行代码

`sglang/srt/function_call/minicpm4_xml_detector.py` 的兜底路径里有这么一行：

```python
has_invalid_param = has_invalid_param
```

`has_invalid_param` 只在**主路径**（XML 解析）的 `if func_node is not None:` 分支里被初始化成 `False`。而主路径的第一句就是 `ET.fromstring(block)`——只要模型输出的参数值里有裸的 `&`（XML 规范里 `&` 必须转义），这句直接抛 `ParseError`，那行初始化永远轮不到执行。

于是进入兜底路径后，上面那个自我赋值是在读一个未绑定的局部变量，抛 `NameError`，被外层的 `except Exception` 吞掉，`parsed_ok` 保持 `False`，整块 `<function>...</function>` **原样**塞回正文。工具没执行，agent 循环终止。

**这条兜底路径本来就是为了处理「XML 不合法但意图明确」的情况而写的**：它用正则 `<param\s+name="([^"]+)">([\s\S]*?)</param>` 抽参数，对裸 `&` 完全无感。逻辑是对的，只是一进门就自爆了。

## 决定性证据

同一段含裸 `&&` 的文本，只改变它在输入里的位置：

```
只有坏块（首块即坏）   -> 解析出调用 0 个，原样泄漏
先放一个合法块，再放它 -> 解析出调用 2 个，包括这个「非法」的，残留为空
```

前面任何一个成功的块都会把 `has_invalid_param` 绑定上，兜底路径于是能正常工作。**所以这个 bug 只咬每条消息里的第一个坏块**——这解释了为什么泄漏看起来时有时无、毫无规律。

单独复刻那行的语义，异常类型确认为 `NameError: name 'has_invalid_param' is not defined`。

## 我早上为什么判错

我读的是 traj_log 里 assistant 消息的 `content`，把它当成了模型的原始输出。但那是**解析器处理之后**返回给客户端的内容。虽然事后看解析器失败时确实是原样放回（`normal_parts.append(block)`），但「原样」这件事我当时没有验证就默认了，方向正好反了：应该先怀疑观测手段，而不是先怀疑模型。

另外我用 `/generate` 裸生成路径做对照也不成立：输出里 ` name="terminal_run_command">` 缺了 `<function`，说明 `<function`/`<param` 是特殊 token，默认解码时被 `skip_special_tokens` 剥掉了。这条路径看不到真实格式。

## 修复与验证

不改 `/user/liuhezi` 的共享 conda 环境，用 `PYTHONPATH` 注入 `sitecustomize.py`：把模块源码读出来，在循环里 `param_invalid = False` 旁边补一行 `has_invalid_param = False`，重新 exec 后替换掉 `detect_and_parse` 方法。部署脚本已带上，服务日志里能看到 `[sglang-patch] minicpm4_xml detect_and_parse patched`。

同一个在线探针（让模型用终端工具跑一条含 `&&` 的命令）：

| | 解析成功 | 泄漏 |
| --- | --- | --- |
| 修复前 | 2/6 | **4/6** |
| 修复后 | **10/10** | **0/10** |

离线用例：

| 输入形态 | XML 合法性 | 修复前 | 修复后 |
| --- | --- | --- | --- |
| 裸 `&&`，标签配平 | 非法（非法字符） | 0 | **1 ✓** |
| CDATA 包裹（模板推荐写法） | 合法 | 1 ✓ | 1 ✓ |
| CDATA 但缺 `</param>` | 非法（标签不匹配） | 0 | 0 |
| `&amp;&amp;` 转义 | 合法 | 1 ✓ | 1 ✓ |

## 责任怎么分

**解析器（主因，已修）**：裸 `&` 是最常见的触发形态，兜底路径本该接住却因未初始化变量失效。这一类丢掉的任务是纯粹的基础设施损失。

**模型（次因，仍在）**：chat template 明确写了「值里含 `<`、`&` 或换行时要包 CDATA」，模型只是**有时**遵守（探针里 6 次有 4 次没包）。严格说这是格式合规问题，但既然解析器已能容错，它不再致命。真正还会致命的是另外两种，样本都很少：

1. **缺 `</param>` 闭合标签**（Toolathlon 里见过 1 例，伴随 CDATA）——标签不匹配，正则兜底也救不了。
2. **编造不存在的工具名**（见过 1 例 `bigquery_search_files`）——按 schema 校验拒绝，解析器行为正确。

## 对分数的影响

08-04 那轮 0/108、以及 08-06 上午带 bug 重跑的那批，**都不能当作模型能力的测量结果**。已带补丁重启服务并重跑全量（exp-name `fixparser`），以这一轮为准。
